import app      from 'ampersand-app';
import BasePage from 'base-page';
import template from 'channels-page.html';

module.exports = BasePage.extend({
    pageTitle: 'Channels Page',
    template: template
});